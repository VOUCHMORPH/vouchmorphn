{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"exec php worker.php batch-worker-${RAILWAY_REPLICA_ID:-$HOSTNAME}\"",
    "restartPolicyType": "ALWAYS"
  }
}
