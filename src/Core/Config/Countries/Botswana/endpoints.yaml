# ============================================
# PARTICIPANT ENDPOINTS
# Connection details, auth, and URL mappings only.
# Capability facts (hold_model, settlement_confirmation,
# source/deposit/cashout) live in participants.yaml.
# ============================================
version: "3.3.1"

ZURUBANK:
  base_url: "https://zurubank-production.up.railway.app/Backend"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    # NOTE: type was "MUTUAL_TLS" in the 3.3.0 file, but this block still
    # carries header_name/secret_source (an API_KEY shape), which mTLS
    # doesn't use. That mismatch will fail auth on its own. Restored to
    # API_KEY to match the fields present — swap back to MUTUAL_TLS only
    # once this block also has cert/key paths instead of a header key.
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "ZURUBANK_API_KEY" }

  oauth:
    client_id: "VOUCHMORPH_APP_ID"
    client_secret: "YOUR_BANK_SECRET"
    redirect_uri: "https://vouchmorphn-production.up.railway.app/api/v1/agent/oauth_callback.php"
    authorization_url: "/api/v1/oauth/authorize.php"
    token_url: "/api/v1/oauth/token.php"
    refresh_url: "/api/v1/oauth/refresh.php"
    revoke_url: "/api/v1/oauth/revoke.php"
    userinfo_url: "/api/v1/oauth/userinfo.php"
    scopes: ["balance", "transactions", "payments"]

  source_linking:
    initiate: "/api/v1/oauth/authorize.php"
    verify: "/api/v1/oauth/token.php"
    refresh: "/api/v1/oauth/refresh.php"
    revoke: "/api/v1/oauth/revoke.php"
    use: "/api/v1/verify_asset.php"

  endpoints:
    source:
      verify_asset: "/api/v1/verify_asset.php"
      place_hold: "/api/v1/hold.php"
      debit_funds: "/api/v1/settlement/notify_debit.php"
      release_hold: "/api/v1/hold.php"
      get_balance: "/api/v1/accounts/balance.php"
      get_transactions: "/api/v1/transactions.php"
    destination_cashout:
      generate_token: "/api/v1/atm/generate_code.php"
      verify_token: "/api/v1/atm/atm_cashout.php"
      confirm_cashout: "/api/v1/atm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/deposit/direct.php"
      verify_account: "/api/v1/verify_asset.php"
    internal:
      sweep: "/api/v1/internal/sweep.php"
    reservation_accounts:
      create_account: "/api/v1/accounts/reservation/create.php"
      get_account_status: "/api/v1/accounts/reservation/status.php"
    common:
      transferWithProof: "/api/v1/transfer.php"
      reverse: "/api/v1/hold.php"
      status: "/api/v1/status"
      check_settlement: "/api/v1/settlement/check.php"
      health: "/health.php"

  callbacks:
    reservation_account_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/reservation_account_confirmed.php"

  message_profile:
    timestamp_header: "X-Timestamp"
    signature_header: "X-Signature"
    trace_id_prefix: "ZURUBANK"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
ABSA:
  base_url: "https://zurubank-production.up.railway.app/Backend"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "ZURUBANK_API_KEY" }

  endpoints:
    source:
      verify_asset: "/api/absa_participant.php?action=verify_asset"
      place_hold: "/api/absa_participant.php?action=place_hold"
      debit_funds: "/api/absa_participant.php?action=debit_funds"
      release_hold: "/api/absa_participant.php?action=release_hold"
      get_balance: "/api/absa_participant.php?action=account_balance"
    destination_cashout:
      generate_token: "/api/absa_participant.php?action=generate_token"
      verify_token: "/api/absa_participant.php?action=verify_token"
      confirm_cashout: "/api/absa_participant.php?action=confirm_cashout"
    destination_deposit:
      process_deposit: "/api/absa_participant.php?action=process_deposit"
      verify_account: "/api/absa_participant.php?action=verify_account"
    internal:
      sweep: "/api/absa_participant.php?action=internal_sweep"
    reservation_accounts:
      create_account: "/api/absa_participant.php?action=create_reservation_account"
      get_account_status: "/api/absa_participant.php?action=reservation_account_status"
    common:
      transferWithProof: "/api/absa_participant.php?action=process_deposit"
      status: "/api/absa_participant.php?action=check_status"
      check_settlement: "/api/absa_participant.php?action=check_settlement"
      health: "/health.php"

  callbacks:
    reservation_account_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/reservation_account_confirmed.php"

  message_profile:
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    trace_id_prefix: "ABSA"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
SACCUSSALIS:
  base_url: "https://saccussalis-production.up.railway.app/backend"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "SACCUSSALIS_API_KEY" }

  source_linking:
    initiate: "/api/v1/auth/initiate.php"
    verify: "/api/v1/auth/verify.php"
    refresh: "/api/v1/auth/refresh.php"
    revoke: "/api/v1/auth/revoke.php"
    use: "/api/v1/verify_asset.php"

  oauth:
    authorization_url: "/oauth/authorize"
    token_url: "/oauth/token"
    refresh_url: "/oauth/refresh"
    revoke_url: "/oauth/revoke"
    userinfo_url: "/oauth/userinfo"
    scopes: ["balance", "transactions", "payments"]

  endpoints:
    source:
      verify_asset: "/api/v1/verify_asset.php"
      place_hold: "/api/v1/hold.php"
      debit_funds: "/api/v1/notify_debit.php"
      release_hold: "/api/v1/hold.php"
      get_balance: "/api/v1/balance.php"
      get_transactions: "/api/v1/transactions"
    destination_cashout:
      generate_token: "/api/v1/atm/generate_code.php"
      verify_token: "/api/v1/atm/verify-token/index.php"
      confirm_cashout: "/api/v1/confirm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/transaction/credit_funds.php"
      verify_account: "/api/v1/verify_account.php"
    internal:
      sweep: "/api/v1/internal/sweep.php"
    reservation_accounts:
      create_account: "/api/v1/accounts/reservation/create.php"
      get_account_status: "/api/v1/accounts/reservation/status.php"
    common:
      transferWithProof: "/api/v1/transfer.php"
      reverse: "/api/v1/hold.php"
      status: "/api/v1/status"
      check_settlement: "/api/v1/settlement/check.php"
      health: "/health.php"

  callbacks:
    settlement_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/settlement_confirmed.php"
    hold_released: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/hold_released.php"
    reservation_account_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/reservation_account_confirmed.php"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "SACCUSSA"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
CAZACOM:
  base_url: "https://cazacom-production.up.railway.app"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "MobileMoneyAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "UPSTREAM_CAZACOM_KEY" }
  endpoints:
    source:
      verify_asset: "/api/v1/mno/verify_wallet.php"
      place_hold: "/api/v1/mno/hold.php"
      debit_funds: "/api/v1/mno/debit.php"
      release_hold: "/api/v1/mno/release_hold.php"
      get_balance: "/api/v1/mno/balance.php"
      get_transactions: "/api/v1/mno/transactions"
    destination_cashout:
      generate_token: "/api/v1/mno/generate_code"
      verify_token: "/api/v1/mno/verify_code"
      confirm_cashout: "/api/v1/mno/confirm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/mno/credit.php"
      verify_account: "/api/v1/mno/verify_wallet.php"
    internal:
      sweep: "/api/v1/mno/internal/sweep"
    reservation_accounts:
      create_account: "/api/v1/mno/reservation/create.php"
      get_account_status: "/api/v1/mno/reservation/status.php"
    common:
      transferWithProof: "/api/v1/mno/transfer"
      reverse: "/api/v1/mno/reverse"
      status: "/api/v1/status"
      check_settlement: "/api/v1/mno/check_settlement"
      health: "/health"

  callbacks:
    reservation_account_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/reservation_account_confirmed.php"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "CAZACOM"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
MTN:
  base_url: "https://cazacom-production.up.railway.app"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "MobileMoneyAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "MTN_VOUCHMORPH_API_KEY" }

  endpoints:
    source:
      verify_asset: "/api/mtn_momo_participant.php?action=verify_asset"
      place_hold: "/api/mtn_momo_participant.php?action=place_hold"
      debit_funds: "/api/mtn_momo_participant.php?action=debit_funds"
      release_hold: "/api/mtn_momo_participant.php?action=release_hold"
      get_balance: "/api/mtn_momo_participant.php?action=wallet_balance"
    destination_deposit:
      process_deposit: "/api/mtn_momo_participant.php?action=process_deposit"
      verify_account: "/api/mtn_momo_participant.php?action=wallet_balance"
    internal:
      sweep: "/api/mtn_momo_participant.php?action=internal_sweep"
    reservation_accounts:
      create_account: "/api/mtn_momo_participant.php?action=create_reservation_account"
      get_account_status: "/api/mtn_momo_participant.php?action=reservation_account_status"
    common:
      transferWithProof: "/api/mtn_momo_participant.php?action=process_deposit"
      status: "/api/mtn_momo_participant.php?action=check_status"
      check_settlement: "/api/mtn_momo_participant.php?action=check_settlement"
      health: "/health"

  callbacks:
    settlement_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/settlement_confirmed.php"
    hold_released: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/hold_released.php"
    reservation_account_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/reservation_account_confirmed.php"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Api-Signature"
    timestamp_header: "X-Api-Timestamp"
    trace_id_prefix: "MTN"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
FNBB_ACQUIRER:
  base_url: "https://zurubank-production.up.railway.app/Backend"
  # TEMPORARY stand-in — real FNBB block below, commented, ready to
  # restore once sandbox access comes through:
  #
  # base_url: "https://api.fnbbotswana.co.bw/acquiring/v1"
  # auth:
  #   type: API_KEY
  #   header_name: "X-API-Key"
  #   secret_source: { type: env_var, name: "FNBB_ACQUIRER_API_KEY" }

  auth:
    type: API_KEY
    header_name: "X-API-Key"
    secret_source:
      type: env_var
      name: "FNBB_ACQUIRER_API_KEY"

  endpoints:
    source:
      verify_asset: "/api/Preauth.php"
      place_hold: "/api/Authorize.php"
      debit_funds: "/api/Capture.php"
      release_hold: "/api/Void.php"
      get_balance: "/n-a"
    destination_deposit:
      process_deposit: "/api/Cardload.php"
    common:
      health: "/health.php"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
CENTRALSWITCH:
  base_url: "https://centralswitch-production.up.railway.app"
  timeout_ms: 10000
  sandbox_mode: true
  adapter_class: "CentralSwitchAdapter"

  auth:
    type: HMAC_SHARED_SECRET
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    secret_source: { type: env_var, name: "CENTRALSWITCH_VOUCHMORPH_SECRET" }

  self_participant_id_source:
    type: env_var
    name: "CENTRALSWITCH_VOUCHMORPH_PARTICIPANT_ID"

  endpoints:
    common:
      submit_transfer: "/api/submit_transfer.php"
      create_quote: "/api/create_quote.php"
      transaction_status: "/api/v1/transactions.php"
      health: "/health"

  message_profile:
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    trace_id_prefix: "CENTRALSWITCH"

  retry_policy:
    max_retries: 2
    retry_delay_ms: 500
    retry_on: [timeout, network_error]

# ------------------------------------------------------------
VOUCHMORPH:
  base_url: "https://vouchmorphn-production.up.railway.app"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "VouchMorphOrchestrationAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "VOUCHMORPH_API_KEY" }

  endpoints:
    common:
      transfer: "/api/v1/swap/execute.php"
      reverse: "/api/v1/swap/cancel.php"
      status: "/api/v1/swap/status.php"
      health: "/health"
      regulatory_report: "/api/regulatory/report"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "VOUCHMOR"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]
