# ============================================
# PARTICIPANT ENDPOINTS
# Connection details, auth, and URL mappings only.
# Capability facts (hold_model, settlement_confirmation,
# source/deposit/cashout) live in participants.yaml.
# ============================================
version: "3.3.0"

ZURUBANK:
  base_url: "https://zurubank-production.up.railway.app/Backend"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    type: MUTUAL_TLS
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "ZURUBANK_VOUCHMORPH_API_KEY" }

  endpoints:
    source:
      verify_asset: "/api/v1/verify_asset.php"
      place_hold: "/api/v1/hold.php"
      debit_funds: "/api/v1/settlement/notify_debit.php"
      release_hold: "/api/v1/hold.php"
      get_balance: "/api/v1/accounts/balance.php"
      get_transactions: "/api/v1/transactions.php"
    destination_cashout:
      generate_token: "/api/v1/atm/generate_code.php"
      verify_token: "/api/v1/atm/atm_cashout.php"
      confirm_cashout: "/api/v1/atm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/deposit/direct.php"
      verify_account: "/api/v1/verify_asset.php"
    internal:
      sweep: "/api/v1/internal/sweep.php"
    common:
      transferWithProof: "/api/v1/transfer.php"
      reverse: "/api/v1/hold.php"
      status: "/api/v1/status"
      check_settlement: "/api/v1/settlement/check.php"
      health: "/health"

  message_profile:
    timestamp_header: "X-Timestamp"
    signature_header: "X-Signature"
    trace_id_prefix: "ZURUBANK"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
ABSA:
  base_url: "https://zurubank-production.up.railway.app/Backend/api"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "ZURUBANK_API_KEY" }

  endpoints:
    source:
      verify_asset: "/absa_participant.php?action=verify_asset"
      place_hold: "/absa_participant.php?action=place_hold"
      debit_funds: "/absa_participant.php?action=debit_funds"
      release_hold: "/absa_participant.php?action=release_hold"
      get_balance: "/absa_participant.php?action=account_balance"
    destination_cashout:
      generate_token: "/absa_participant.php?action=generate_token"
      verify_token: "/absa_participant.php?action=verify_token"
      confirm_cashout: "/absa_participant.php?action=confirm_cashout"
    destination_deposit:
      process_deposit: "/absa_participant.php?action=process_deposit"
      verify_account: "/absa_participant.php?action=verify_account"
    internal:
      sweep: "/absa_participant.php?action=internal_sweep"
    common:
      transferWithProof: "/absa_participant.php?action=process_deposit"
      status: "/absa_participant.php?action=check_status"
      check_settlement: "/absa_participant.php?action=check_settlement"
      health: "/health"

  message_profile:
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    trace_id_prefix: "ABSA"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
SACCUSSALIS:
  base_url: "https://saccussalis-production.up.railway.app/backend"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "GenericFinancialInstitutionAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "SACCUSSALIS_VOUCHMORPH_API_KEY" }

  endpoints:
    source:
      verify_asset: "/api/v1/verify_asset.php"
      place_hold: "/api/v1/hold.php"
      debit_funds: "/api/v1/notify_debit.php"
      release_hold: "/api/v1/hold.php"
      get_balance: "/api/v1/balance.php"
      get_transactions: "/api/v1/transactions"
    destination_cashout:
      generate_token: "/api/v1/atm/generate_code.php"
      verify_token: "/api/v1/atm/verify-token/index.php"
      confirm_cashout: "/api/v1/confirm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/transaction/credit_funds.php"
      verify_account: "/api/v1/verify_account.php"
    internal:
      sweep: "/api/v1/internal/sweep.php"
    common:
      transferWithProof: "/api/v1/transfer.php"
      reverse: "/api/v1/hold.php"
      status: "/api/v1/status"
      check_settlement: "/api/v1/settlement/check.php"
      health: "/health"

  callbacks:
    settlement_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/settlement_confirmed.php"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "SACCUSSA"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
CAZACOM:
  base_url: "https://cazacom-production.up.railway.app"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "MobileMoneyAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "UPSTREAM_CAZACOM_KEY" }
  endpoints:
    source:
      verify_asset: "/api/v1/mno/verify_wallet.php"
      place_hold: "/api/v1/mno/hold.php"
      debit_funds: "/api/v1/mno/debit.php"
      release_hold: "/api/v1/mno/release_hold.php"
      get_balance: "/api/v1/mno/balance.php"
      get_transactions: "/api/v1/mno/transactions"
    destination_cashout:
      generate_token: "/api/v1/mno/generate_code"
      verify_token: "/api/v1/mno/verify_code"
      confirm_cashout: "/api/v1/mno/confirm_cashout.php"
    destination_deposit:
      process_deposit: "/api/v1/mno/credit.php"
      verify_account: "/api/v1/mno/verify_wallet.php"
    internal:
      sweep: "/api/v1/mno/internal/sweep"
    common:
      transferWithProof: "/api/v1/mno/transfer"
      reverse: "/api/v1/mno/reverse"
      status: "/api/v1/status"
      check_settlement: "/api/v1/mno/check_settlement"
      health: "/health"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "CAZACOM"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
MTN:
  base_url: "https://cazacom-production.up.railway.app" 
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "MobileMoneyAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "MTN_VOUCHMORPH_API_KEY" }

  endpoints:
    source:
      verify_asset: "/api/mtn_momo_participant.php?action=verify_asset"
      place_hold: "/api/mtn_momo_participant.php?action=place_hold"
      debit_funds: "/api/mtn_momo_participant.php?action=debit_funds"
      release_hold: "/api/mtn_momo_participant.php?action=release_hold"
      get_balance: "/api/mtn_momo_participant.php?action=wallet_balance"
    destination_deposit:
      process_deposit: "/api/mtn_momo_participant.php?action=process_deposit"
      verify_account: "/api/mtn_momo_participant.php?action=wallet_balance"
    internal:
      sweep: "/api/mtn_momo_participant.php?action=internal_sweep"
    common:
      transferWithProof: "/api/mtn_momo_participant.php?action=process_deposit"
      status: "/api/mtn_momo_participant.php?action=check_status"
      check_settlement: "/api/mtn_momo_participant.php?action=check_settlement"
      health: "/health"

  callbacks:
    settlement_confirmed: "https://vouchmorphn-production.up.railway.app/api/v1/callbacks/settlement_confirmed.php"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Api-Signature"
    timestamp_header: "X-Api-Timestamp"
    trace_id_prefix: "MTN"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
FNBB_ACQUIRER:
  base_url: "https://zurubank-production.up.railway.app/Backend/api"
  # TEMPORARY stand-in — real FNBB block below, commented, ready to
  # restore once sandbox access comes through:
  #
  # base_url: "https://api.fnbbotswana.co.bw/acquiring/v1"
  # auth:
  #   type: API_KEY
  #   header_name: "X-API-Key"
  #   secret_source: { type: env_var, name: "FNBB_ACQUIRER_API_KEY" }

  auth:
    type: API_KEY
    header_name: "X-API-Key"
    secret_source:
      type: env_var
      name: "FNBB_ACQUIRER_API_KEY"

  endpoints:
    source:
      verify_asset: "/preauth.php"
      place_hold: "/authorize.php"
      debit_funds: "/capture.php"
      release_hold: "/void.php"
      get_balance: "/n-a"
    destination_deposit:
      process_deposit: "/card-load.php"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]

# ------------------------------------------------------------
CENTRALSWITCH:
  base_url: "https://centralswitch-production.up.railway.app"
  timeout_ms: 10000
  sandbox_mode: true
  adapter_class: "CentralSwitchAdapter"

  auth:
    type: HMAC_SHARED_SECRET
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    secret_source: { type: env_var, name: "CENTRALSWITCH_VOUCHMORPH_SECRET" }

  self_participant_id_source:
    type: env_var
    name: "CENTRALSWITCH_VOUCHMORPH_PARTICIPANT_ID"

  endpoints:
    common:
      submit_transfer: "/api/submit_transfer.php"
      create_quote: "/api/create_quote.php"
      transaction_status: "/api/v1/transactions.php"
      health: "/health"

  message_profile:
    timestamp_header: "X-Api-Timestamp"
    signature_header: "X-Api-Signature"
    trace_id_prefix: "CENTRALSWITCH"

  retry_policy:
    max_retries: 2
    retry_delay_ms: 500
    retry_on: [timeout, network_error]

# ------------------------------------------------------------
VOUCHMORPH:
  base_url: "https://vouchmorphn-production.up.railway.app"
  timeout_ms: 5000
  sandbox_mode: true
  adapter_class: "VouchMorphOrchestrationAdapter"

  auth:
    type: API_KEY
    header_name: "X-API-KEY"
    secret_source: { type: env_var, name: "VOUCHMORPH_API_KEY" }

  endpoints:
    common:
      transfer: "/api/v1/swap/execute.php"
      reverse: "/api/v1/swap/cancel.php"
      status: "/api/v1/swap/status.php"
      health: "/health"
      regulatory_report: "/api/regulatory/report"

  message_profile:
    correlation_id_header: "X-Correlation-ID"
    idempotency_key_header: "X-Idempotency-Key"
    signature_header: "X-Signature"
    timestamp_header: "X-Timestamp"
    trace_id_prefix: "VOUCHMOR"

  retry_policy:
    max_retries: 3
    retry_delay_ms: 1000
    retry_on: [timeout, 5xx, network_error]
